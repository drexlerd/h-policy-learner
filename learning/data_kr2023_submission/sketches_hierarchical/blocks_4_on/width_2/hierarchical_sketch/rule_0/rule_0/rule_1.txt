(:policy
(:boolean_features "b_empty(r_and(r_primitive(on,0,1),r_primitive(on_g,0,1)))")
(:numerical_features "n_count(c_not(c_primitive(clear,0)))")
(:rule (:conditions (:c_n_eq 0)) (:effects (:e_b_neg 0)))
)