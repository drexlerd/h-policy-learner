(:policy
(:boolean_features "b_empty(c_some(r_inverse(r_primitive(carry,0,1)),c_some(r_primitive(at_g,0,1),c_primitive(at-robby,0))))")
(:numerical_features "n_count(c_primitive(free,0))")
(:rule (:conditions (:c_b_pos 0) (:c_n_gt 0)) (:effects (:e_n_dec 0)))
)