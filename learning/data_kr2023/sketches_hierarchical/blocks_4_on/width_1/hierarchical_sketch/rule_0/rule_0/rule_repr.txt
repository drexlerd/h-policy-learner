(:policy
(:rule (:conditions (:c_b_pos "b_empty(r_and(r_primitive(on,0,1),r_primitive(on_g,0,1)))") (:c_n_eq "n_count(c_some(r_inverse(r_primitive(on_g,0,1)),c_primitive(on-table,0)))")) (:effects (:e_b_neg "b_empty(r_and(r_primitive(on,0,1),r_primitive(on_g,0,1)))")))
)