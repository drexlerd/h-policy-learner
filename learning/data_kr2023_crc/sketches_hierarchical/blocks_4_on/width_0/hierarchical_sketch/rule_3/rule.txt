(:policy
(:boolean_features "b_nullary(arm-empty)" "b_empty(r_and(r_primitive(on,0,1),r_primitive(on_g,0,1)))" "b_empty(c_and(c_primitive(holding,0),c_some(r_primitive(on_g,0,1),c_primitive(clear,0))))")
(:numerical_features "n_count(r_primitive(on,0,1))")
(:rule (:conditions (:c_b_pos 0) (:c_n_eq 0) (:c_b_pos 1)) (:effects (:e_b_pos 1) (:e_b_neg 2)))
)