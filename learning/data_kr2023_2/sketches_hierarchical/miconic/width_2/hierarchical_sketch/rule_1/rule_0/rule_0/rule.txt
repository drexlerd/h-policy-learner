(:policy
(:boolean_features "b_empty(c_some(r_primitive(origin,0,1),c_primitive(lift-at,0)))" "b_empty(c_primitive(boarded,0))")
(:numerical_features )
(:rule (:conditions (:c_b_neg 0)) (:effects (:e_b_neg 1)))
)