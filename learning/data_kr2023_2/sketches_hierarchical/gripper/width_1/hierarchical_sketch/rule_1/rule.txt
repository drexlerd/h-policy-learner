(:policy
(:boolean_features "b_empty(r_primitive(carry,0,1))")
(:numerical_features "n_count(c_some(r_primitive(at,0,1),c_one_of(roomb)))")
(:rule (:conditions (:c_b_neg 0)) (:effects (:e_n_inc 0)))
)