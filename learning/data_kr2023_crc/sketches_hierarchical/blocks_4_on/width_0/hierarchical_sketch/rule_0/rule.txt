(:policy
(:boolean_features "b_empty(r_and(r_primitive(on,0,1),r_primitive(on_g,0,1)))" "b_empty(c_and(c_primitive(holding,0),c_some(r_primitive(on_g,0,1),c_primitive(clear,0))))")
(:numerical_features )
(:rule (:conditions (:c_b_pos 0) (:c_b_neg 1)) (:effects (:e_b_neg 0) (:e_b_pos 1)))
)