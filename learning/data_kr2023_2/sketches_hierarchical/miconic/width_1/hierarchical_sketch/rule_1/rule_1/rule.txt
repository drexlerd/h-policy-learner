(:policy
(:boolean_features "b_empty(c_primitive(boarded,0))")
(:numerical_features "n_count(c_and(c_primitive(boarded,0),c_some(r_primitive(destin,0,1),c_primitive(lift-at,0))))")
(:rule (:conditions (:c_b_neg 0) (:c_n_eq 0)) (:effects (:e_n_inc 0)))
)