(:policy
(:boolean_features )
(:numerical_features "n_count(c_some(r_inverse(r_transitive_closure(r_primitive(link,0,1))),c_some(r_inverse(r_primitive(at,0,1)),c_primitive(man,0))))" "n_count(r_restrict(r_primitive(carrying,0,1),c_primitive(useable,0)))")
(:rule (:conditions (:c_n_eq 0)) (:effects (:e_n_dec 1)))
)