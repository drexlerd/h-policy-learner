(:policy
(:rule (:conditions (:c_n_eq "n_count(c_primitive(boarded,0))") (:c_n_eq "n_count(c_some(r_primitive(origin,0,1),c_primitive(lift-at,0)))") (:c_n_eq "n_count(c_and(c_primitive(boarded,0),c_some(r_primitive(destin,0,1),c_primitive(lift-at,0))))")) (:effects (:e_n_inc "n_count(c_some(r_primitive(origin,0,1),c_primitive(lift-at,0)))")))
(:rule (:conditions (:c_n_eq "n_count(c_some(r_primitive(origin,0,1),c_primitive(lift-at,0)))") (:c_n_gt "n_count(c_and(c_primitive(boarded,0),c_some(r_primitive(destin,0,1),c_primitive(lift-at,0))))")) (:effects (:e_n_dec "n_count(c_primitive(boarded,0))")))
(:rule (:conditions (:c_n_gt "n_count(c_primitive(boarded,0))") (:c_n_eq "n_count(c_and(c_primitive(boarded,0),c_some(r_primitive(destin,0,1),c_primitive(lift-at,0))))")) (:effects (:e_n_inc "n_count(c_and(c_primitive(boarded,0),c_some(r_primitive(destin,0,1),c_primitive(lift-at,0))))")))
(:rule (:conditions (:c_n_gt "n_count(c_some(r_primitive(origin,0,1),c_primitive(lift-at,0)))")) (:effects (:e_n_inc "n_count(c_primitive(boarded,0))")))
)