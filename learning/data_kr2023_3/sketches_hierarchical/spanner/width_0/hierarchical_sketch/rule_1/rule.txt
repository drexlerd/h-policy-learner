(:policy
(:boolean_features "b_empty(c_some(r_primitive(at,0,1),c_all(r_inverse(r_primitive(at,0,1)),c_primitive(man,0))))")
(:numerical_features "n_count(c_some(r_inverse(r_transitive_closure(r_primitive(link,0,1))),c_some(r_inverse(r_primitive(at,0,1)),c_top)))")
(:rule (:conditions (:c_b_neg 0)) (:effects (:e_n_dec 0)))
)